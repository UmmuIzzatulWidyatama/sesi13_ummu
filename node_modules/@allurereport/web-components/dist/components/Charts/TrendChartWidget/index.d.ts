import { ChartMode } from "@allurereport/web-commons";
import type { CSSProperties } from "preact/compat";
import type { Serie } from "@/components/Charts/TrendChart";
interface TrendChartWidgetPropsTranslations {
    "no-results": string;
}
interface TrendChartWidgetProps<TSlice = {
    metadata: {
        executionId: string;
    };
}> {
    title: string;
    mode: ChartMode;
    items: readonly Serie[];
    slices: readonly TSlice[];
    min: number;
    max: number;
    height?: CSSProperties["height"];
    width?: CSSProperties["width"];
    rootAriaLabel?: string;
    translations: TrendChartWidgetPropsTranslations;
}
export declare const TrendChartWidget: ({ title, mode, items, slices, min, max, height, width, rootAriaLabel, translations, }: TrendChartWidgetProps) => import("preact/compat").JSX.Element;
export {};
//# sourceMappingURL=index.d.ts.map